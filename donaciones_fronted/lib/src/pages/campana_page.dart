import 'package:flutter/material.dart';

class CampanaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Campaña Page"),
    );
  }
}
