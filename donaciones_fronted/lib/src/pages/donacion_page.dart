import 'package:flutter/material.dart';

class DonacionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Donacion Page"),
    );
  }
}
